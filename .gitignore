# Claude Code local
# .claude/ holds local symlinks to agents/, skills/, commands/ for in-repo testing.
# Source of truth lives at the top-level directories; .claude/ is a deployment shim.
.claude/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Run outputs (keep checked-in examples but ignore ad-hoc runs)
runs/
*.tmp
